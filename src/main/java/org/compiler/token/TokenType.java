package org.compiler.token;

/**
 * The types of allowed tokens
 */
public enum TokenType {
    _exit, int_lit, semi, open_paren, close_paren, ident, let, eq, plus, star, minus, slash, comment, open_curly, close_curly
}
